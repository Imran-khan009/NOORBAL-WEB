# 1. API Route Redirection: Forward all /api/* requests to Netlify Serverless Function
/api/*    /.netlify/functions/api/:splat    200!

# 2. SPA Fallback: Ensures customer & admin routes load /index.html cleanly without 404
/*        /index.html                       200

